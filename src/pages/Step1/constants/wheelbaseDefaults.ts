/**
 * Typical wheelbase values in meters
 * Used as defaults when generating fields if the user hasn't entered anything yet
 */

/** For the truck between axles 1–2 or 2–3 (e.g., KAMAZ, MAN, Volvo) */
export const DEFAULT_TRUCK_WHEELBASE_METERS = 3.6;

/** For the trailer between axles (e.g., Schmitz, Krone, and others) */
export const DEFAULT_TRAILER_WHEELBASE_METERS = 1.3;
